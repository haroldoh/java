//Swap the elements so that the array becomes reversed

public class Main
{
	public static void main(String[] args) {
		int[] arr = {0,1,2,3};
		for(int i=0;i<arr.length/2;i++){
		    int temp = arr[i];
		    arr[i] = arr[arr.length - i - 1];
		    arr[arr.length - i - 1] = temp;
		}
	}
}


